# Fish machine state — not portable
fish_variables
fish_history

# Fisher-managed plugin artifacts (regenerated by `fisher update` from
# fish_plugins on any machine — do not commit, like node_modules).
functions/fisher.fish
functions/fenv.fish
functions/fenv.main.fish
functions/_autopair_*.fish
conf.d/autopair.fish
