// ═══════════════════════════════════════════════════════════════════════════════════════════
// NIRI CONFIG — ROOT
// Split into modules/. Edit those files, not this one.
// Include paths resolve relative to this file's directory (~/.config/niri/).
// ═══════════════════════════════════════════════════════════════════════════════════════════

include "modules/input.kdl"
include "modules/outputs.kdl"
include "modules/layout.kdl"
include "modules/window-rules.kdl"
include "modules/environment.kdl"
include "modules/startup.kdl"
include "modules/binds.kdl"
include "modules/misc.kdl"

// DankMaterialShell-managed includes (auto-generated — do not hand-edit)
include "dms/cursor.kdl"
include "dms/windowrules.kdl"
include "dms/outputs.kdl"

binds {
    // >>> jcode launch hotkeys (managed) >>>
    Super+Semicolon hotkey-overlay-title="jcode: home" { spawn "sh" "-c" "if [ -d '/home/nova' ]; then cd '/home/nova'; else cd \"$HOME\"; fi; exec 'ghostty' '/var/tmp/jcode-build/src/target/release/jcode' --spawn-hotkey 'cmd+;'"; }
    Super+Apostrophe hotkey-overlay-title="jcode: last project" { spawn "sh" "-c" "if [ -d '/home/nova' ]; then cd '/home/nova'; else cd \"$HOME\"; fi; exec 'ghostty' '/var/tmp/jcode-build/src/target/release/jcode' --spawn-hotkey 'cmd+'\\'''"; }
    Super+Shift+Apostrophe hotkey-overlay-title="jcode: self-dev (self-dev)" { spawn "sh" "-c" "if [ -d '/home/nova' ]; then cd '/home/nova'; else cd \"$HOME\"; fi; exec 'ghostty' '/var/tmp/jcode-build/src/target/release/jcode' --spawn-hotkey 'cmd+shift+'\\''' self-dev"; }
    // <<< jcode launch hotkeys (managed) <<<
}
