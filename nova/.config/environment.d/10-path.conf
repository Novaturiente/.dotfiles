# PATH for graphical session (Ly -> systemd user manager -> niri).
# Login shells add these themselves; apps spawned from niri binds do not.
PATH=$HOME/.local/bin:$HOME/.cargo/bin:$HOME/.npm-global/bin:$HOME/.local/share/go/bin:$PATH
