[mgr]
ratio = [1, 3, 4]

# git.yazi: show git status as linemode signs next to every file
# (clean + modified files both listed; modified ones get a sign)
[[plugin.prepend_fetchers]]
url   = "*"
run   = "git"
group = "git"

[[plugin.prepend_fetchers]]
url   = "*/"
run   = "git"
group = "git"
